/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

// next.config.js
module.exports = {
    images: {
      domains: ['raw.githubusercontent.com'], // Add any other domains you want to allow
    },
  };
  
